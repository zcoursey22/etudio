import { Heading } from "@chakra-ui/react";
import { getTitle } from "../utils";

export const Training = () => {
  return (
    <>
      <title>{getTitle("Training")}</title>
      <Heading>Training</Heading>
    </>
  );
};
